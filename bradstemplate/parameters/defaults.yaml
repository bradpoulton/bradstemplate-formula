# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: bradstemplate
  rootgroup: root
  config: '/etc/bradstemplate'
  service:
    name: bradstemplate
  subcomponent:
    config: '/etc/bradstemplate-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
